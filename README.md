# Setup

Hosting setup for demos.tf

## Usage

Create a `.env` file with the following values:

- HOST
- PORT
- HTTPS_PORT
- EMAIL
- DB_PASSWORD

Start the containers:

```bash
docker-compose up -d
```
